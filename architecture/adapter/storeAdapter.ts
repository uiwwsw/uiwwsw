// import { StorageService } from '@/application/ports';
import { BehaviorSubject } from 'rxjs';
// export class StoreAdapter implements StorageService {
// 	get<T>(key: string): T {
// 		const value = cookiejs.get(key) as unknown
// 		return value as T
// 	}
// 	set({key, value, time}: {key: string, value: unknown, time?: number}): void {
// 		if (value) cookiejs.set(key, JSON.stringify(value),time)
// 	}
// 	remove(key: string) {
// 		cookiejs.remove(key)
// 	}
// 	clear(): void {
// 		cookiejs.clear()
// 	}
// }
